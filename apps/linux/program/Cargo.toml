[package]
name = "intelli-light-linux"
version = "0.1.1"
edition = "2021"
description = "Contract-driven headless core for intelli-light"
license = "MIT"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
